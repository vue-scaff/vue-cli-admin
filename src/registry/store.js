export default () => {
  const name = "global";

  const state = {
    screen: {
      width: 375,
      height: 667
    }
  };

  const mutations = {};

  const actions = {};

  return {
    name,
    state,
    mutations,
    actions
  };
};
